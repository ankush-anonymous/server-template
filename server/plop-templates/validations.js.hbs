const { check:{{camelCase name}}Check } = require('express-validator');

// TO-DO : Change this
exports.getByIdValidation = [
{{camelCase name}}check('shipmentId').isLength({min:1}).withMessage('ShipmentId could not be blank'),
];